from collections import deque
from matcher import match

def basic_to_elem(basic_stack, elementary_stack):
    #Match elementary rule on basic_stack
    # for rule in ElementaryRules:
    #     if match(basic_stack, rule):
    #         new_elem = ElementaryBlock()
    #Push elementary event
    # elementary_stack.append(new_elem)
    raise NotImplementedError